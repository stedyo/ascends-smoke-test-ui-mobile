import { BuilderCtx } from '../context';
import { BaseAndroidJob } from '../platforms/android/baseJob';
import { BaseiOSJob } from '../platforms/ios/baseJob';
declare type Job = BaseAndroidJob | BaseiOSJob;
interface BuildDirectories {
    buildDirectory: string;
    nativeProjectDirectory: string;
}
declare function setup(ctx: BuilderCtx<Job>): Promise<BuildDirectories>;
export { setup };
